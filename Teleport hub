-- 📌 TP METHODS + AUTO ACCEPT PET GIFT SYSTEM (WITH TOGGLE + NOTIFICATION) + DRAGGABLE GUI
-- LocalScript (StarterPlayerScripts or StarterGui)

local Players = game:GetService("Players")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local LocalPlayer = Players.LocalPlayer

-- ==== UI SETUP ====
local screenGui = Instance.new("ScreenGui")
screenGui.Name = "TPMethods"
screenGui.Parent = LocalPlayer:WaitForChild("PlayerGui")

-- Main frame
local frame = Instance.new("Frame")
frame.Size = UDim2.new(0, 240, 0, 380)
frame.Position = UDim2.new(0, 40, 0, 120)
frame.BackgroundColor3 = Color3.fromRGB(0, 85, 170)
frame.BorderSizePixel = 0
frame.Active = true
frame.Draggable = true
frame.Parent = screenGui

Instance.new("UICorner", frame).CornerRadius = UDim.new(0,12)

local frameStroke = Instance.new("UIStroke")
frameStroke.Color = Color3.fromRGB(255,255,255)
frameStroke.Thickness = 2
frameStroke.Parent = frame

-- Drop shadow
local shadow = Instance.new("ImageLabel")
shadow.Size = UDim2.new(1,20,1,20)
shadow.Position = UDim2.new(0,-10,0,-10)
shadow.BackgroundTransparency = 1
shadow.Image = "rbxassetid://1316045217"
shadow.ImageColor3 = Color3.fromRGB(0,0,0)
shadow.ImageTransparency = 0.5
shadow.ScaleType = Enum.ScaleType.Slice
shadow.SliceCenter = Rect.new(10,10,118,118)
shadow.ZIndex = 0
shadow.Parent = frame

-- Title
local title = Instance.new("TextLabel")
title.Size = UDim2.new(1, 0, 0, 35)
title.Text = "TP Methods"
title.TextColor3 = Color3.fromRGB(255,255,255)
title.Font = Enum.Font.SourceSansBold
title.TextSize = 20
title.BackgroundColor3 = Color3.fromRGB(0, 65, 140)
title.BorderSizePixel = 0
title.Parent = frame
Instance.new("UICorner", title).CornerRadius = UDim.new(0,12)

-- Auto Accept Toggle Button
local autoAccept = false
local toggleBtn = Instance.new("TextButton")
toggleBtn.Size = UDim2.new(1, -10, 0, 30)
toggleBtn.Position = UDim2.new(0,5,0,40)
toggleBtn.Text = "Auto Accept: OFF"
toggleBtn.BackgroundColor3 = Color3.fromRGB(50,120,200)
toggleBtn.TextColor3 = Color3.fromRGB(255,255,255)
toggleBtn.Font = Enum.Font.SourceSansBold
toggleBtn.TextSize = 16
toggleBtn.Parent = frame

Instance.new("UICorner", toggleBtn).CornerRadius = UDim.new(0,8)
local toggleStroke = Instance.new("UIStroke")
toggleStroke.Color = Color3.fromRGB(255,255,255)
toggleStroke.Thickness = 1.5
toggleStroke.Parent = toggleBtn

toggleBtn.MouseButton1Click:Connect(function()
    autoAccept = not autoAccept
    if autoAccept then
        toggleBtn.Text = "Auto Accept: ON"
        toggleBtn.BackgroundColor3 = Color3.fromRGB(0,170,255)
    else
        toggleBtn.Text = "Auto Accept: OFF"
        toggleBtn.BackgroundColor3 = Color3.fromRGB(50,120,200)
    end
end)

-- Scrolling list for players
local scrollingFrame = Instance.new("ScrollingFrame")
scrollingFrame.Size = UDim2.new(1, -10, 1, -80)
scrollingFrame.Position = UDim2.new(0,5,0,75)
scrollingFrame.CanvasSize = UDim2.new(0,0,0,0)
scrollingFrame.ScrollBarThickness = 6
scrollingFrame.BackgroundTransparency = 0.2
scrollingFrame.BackgroundColor3 = Color3.fromRGB(0,65,140)
scrollingFrame.Parent = frame

Instance.new("UICorner", scrollingFrame).CornerRadius = UDim.new(0,8)

-- Function to refresh player list
local function refreshPlayers()
    scrollingFrame:ClearAllChildren()
    local y = 0
    for _, player in pairs(Players:GetPlayers()) do
        if player ~= LocalPlayer then
            local btn = Instance.new("TextButton")
            btn.Size = UDim2.new(1, -5, 0, 30)
            btn.Position = UDim2.new(0,0,0,y)
            btn.Text = "Teleport to " .. player.Name
            btn.BackgroundColor3 = Color3.fromRGB(0,100,200)
            btn.TextColor3 = Color3.fromRGB(255,255,255)
            btn.Font = Enum.Font.SourceSans
            btn.TextSize = 16
            btn.Parent = scrollingFrame

            Instance.new("UICorner", btn).CornerRadius = UDim.new(0,6)
            local btnStroke = Instance.new("UIStroke")
            btnStroke.Color = Color3.fromRGB(255,255,255)
            btnStroke.Thickness = 1
            btnStroke.Parent = btn

            btn.MouseButton1Click:Connect(function()
                if LocalPlayer.Character and player.Character then
                    local hrp = LocalPlayer.Character:FindFirstChild("HumanoidRootPart")
                    local targetHrp = player.Character:FindFirstChild("HumanoidRootPart")
                    if hrp and targetHrp then
                        hrp.CFrame = targetHrp.CFrame + Vector3.new(0,3,0)
                    end
                end
            end)

            y = y + 35
        end
    end
    scrollingFrame.CanvasSize = UDim2.new(0,0,0,y)
end

Players.PlayerAdded:Connect(refreshPlayers)
Players.PlayerRemoving:Connect(refreshPlayers)
refreshPlayers()

-- ==== NOTIFICATION FUNCTION ====
local function showNotification(msg, color)
    local notif = Instance.new("TextLabel")
    notif.Size = UDim2.new(0, 220, 0, 40)
    notif.Position = UDim2.new(0.5, -110, 0, 20)
    notif.BackgroundColor3 = color or Color3.fromRGB(0, 150, 80)
    notif.TextColor3 = Color3.fromRGB(255, 255, 255)
    notif.Font = Enum.Font.SourceSansBold
    notif.TextSize = 18
    notif.Text = msg
    notif.BackgroundTransparency = 0
    notif.Parent = screenGui
    notif.ZIndex = 10
    Instance.new("UICorner", notif).CornerRadius = UDim.new(0, 10)

    task.delay(3, function()
        for i = 0, 1, 0.1 do
            notif.BackgroundTransparency = i
            notif.TextTransparency = i
            task.wait(0.05)
        end
        notif:Destroy()
    end)
end

-- ==== AUTO ACCEPT PET GIFT SYSTEM ====
local giftEvent = ReplicatedStorage:FindFirstChild("GiftEvent", true)

if giftEvent and giftEvent:IsA("RemoteEvent") then
    print("✅ GiftEvent found at path:", giftEvent:GetFullName())

    giftEvent.OnClientEvent:Connect(function(...)
        local args = {...}
        print("📦 Gift received:", unpack(args))

        local senderName, giftName = "Unknown Sender", "Unknown Gift"
        for _, arg in ipairs(args) do
            if typeof(arg) == "Instance" and arg:IsA("Player") then
                senderName = arg.Name
            elseif typeof(arg) == "string" then
                giftName = arg
            elseif typeof(arg) == "table" and arg.Name then
                giftName = arg.Name
            end
        end

        if autoAccept then
            giftEvent:FireServer(unpack(args))
            print("✅ AUTO ACCEPTED ".. giftName .." from ".. senderName)
            showNotification("🎁 AUTO ACCEPTED: ".. giftName .." from ".. senderName, Color3.fromRGB(0,170,80))
        else
            print("❌ Gift ignored from ".. senderName .." (".. giftName ..")")
            showNotification("📦 GIFT IGNORED: ".. giftName .." from ".. senderName, Color3.fromRGB(255,140,0))
        end
    end)
else
    warn("❌ GiftEvent not found. Double-check path in ReplicatedStorage.")
end
