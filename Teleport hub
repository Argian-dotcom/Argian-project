-- 📌 TP METHODS + PET GIFT SYSTEM (NO AUTO ACCEPT TOGGLE) + NOTIFICATION + DRAGGABLE GUI
-- LocalScript (for your own Roblox game)

local Players = game:GetService("Players")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local LocalPlayer = Players.LocalPlayer

-- ==== UI SETUP ====
local screenGui = Instance.new("ScreenGui")
screenGui.Name = "TPMethods"
screenGui.Parent = LocalPlayer:WaitForChild("PlayerGui")

-- Main frame
local frame = Instance.new("Frame")
frame.Size = UDim2.new(0, 240, 0, 380)
frame.Position = UDim2.new(0, 40, 0, 120)
frame.BackgroundColor3 = Color3.fromRGB(0, 85, 170)
frame.BorderSizePixel = 0
frame.Active = true
frame.Draggable = true
frame.Parent = screenGui

Instance.new("UICorner", frame).CornerRadius = UDim.new(0,12)

local frameStroke = Instance.new("UIStroke")
frameStroke.Color = Color3.fromRGB(255,255,255)
frameStroke.Thickness = 2
frameStroke.Parent = frame

-- Title
local title = Instance.new("TextLabel")
title.Size = UDim2.new(1, 0, 0, 35)
title.Text = "TP Methods"
title.TextColor3 = Color3.fromRGB(255,255,255)
title.Font = Enum.Font.SourceSansBold
title.TextSize = 20
title.BackgroundColor3 = Color3.fromRGB(0, 65, 140)
title.BorderSizePixel = 0
title.Parent = frame
Instance.new("UICorner", title).CornerRadius = UDim.new(0,12)

-- ==== NOTIFICATION FUNCTION ====
local function showNotification(msg, color)
    local notif = Instance.new("TextLabel")
    notif.Size = UDim2.new(0, 220, 0, 40)
    notif.Position = UDim2.new(0.5, -110, 0, 20)
    notif.BackgroundColor3 = color or Color3.fromRGB(0, 150, 80)
    notif.TextColor3 = Color3.fromRGB(255, 255, 255)
    notif.Font = Enum.Font.SourceSansBold
    notif.TextSize = 18
    notif.Text = msg
    notif.Parent = screenGui
    notif.ZIndex = 10
    Instance.new("UICorner", notif).CornerRadius = UDim.new(0, 10)

    task.delay(3, function()
        for i = 0, 1, 0.1 do
            notif.BackgroundTransparency = i
            notif.TextTransparency = i
            task.wait(0.05)
        end
        notif:Destroy()
    end)
end

-- ==== PET GIFT SYSTEM (NO AUTO ACCEPT) ====
local giftEvent = ReplicatedStorage:FindFirstChild("GiftEvent")

if giftEvent and giftEvent:IsA("RemoteEvent") then
    print("✅ GiftEvent found at path:", giftEvent:GetFullName())

    giftEvent.OnClientEvent:Connect(function(senderName, giftName)
        print("📦 Gift received:", senderName, giftName)

        -- Laging IGNORED kasi wala na auto accept
        print("❌ Gift ignored from ".. senderName .." (".. giftName ..")")
        showNotification("📦 GIFT IGNORED: ".. giftName .." from ".. senderName, Color3.fromRGB(255,140,0))
    end)
else
    warn("⚠️ GiftEvent not found. Make sure you created one in ReplicatedStorage.")
end
