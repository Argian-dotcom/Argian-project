-- 🤑 EZ PETS 🤑 TELEPORT HUB
-- (KEY SYSTEM • MAIN GUI • TELEPORT • NO HIDE NAME • NO JUMPSCARE)

local Players = game:GetService("Players")
local LocalPlayer = Players.LocalPlayer
local TweenService = game:GetService("TweenService")
local UIS = game:GetService("UserInputService")
local CoreGui = game:GetService("CoreGui")

-- ========= UI ROOT =========
local screenGui = Instance.new("ScreenGui")
screenGui.Name = "EZPETS_GUI"
screenGui.ResetOnSpawn = false
screenGui.Parent = CoreGui
screenGui.Enabled = false -- default off, wait for key

-- ========= MAIN FRAME =========
local mainFrame = Instance.new("Frame")
mainFrame.Size = UDim2.new(0, 400, 0, 300)
mainFrame.Position = UDim2.new(0.5, -200, 0.5, -150)
mainFrame.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
mainFrame.BorderSizePixel = 0
mainFrame.Active = true
mainFrame.Draggable = true
mainFrame.Parent = screenGui

local UICorner = Instance.new("UICorner", mainFrame)
UICorner.CornerRadius = UDim.new(0, 12)

local titleBar = Instance.new("TextLabel")
titleBar.Size = UDim2.new(1, 0, 0, 40)
titleBar.BackgroundColor3 = Color3.fromRGB(50, 50, 50)
titleBar.Text = "🤑 EZ PETS 🤑 TELEPORT HUB"
titleBar.TextColor3 = Color3.new(1, 1, 1)
titleBar.Font = Enum.Font.SourceSansBold
titleBar.TextSize = 20
titleBar.Parent = mainFrame

local UICorner2 = Instance.new("UICorner", titleBar)
UICorner2.CornerRadius = UDim.new(0, 12)

-- Minimize Button
local closeBtn = Instance.new("TextButton")
closeBtn.Size = UDim2.new(0, 40, 1, 0)
closeBtn.Position = UDim2.new(1, -40, 0, 0)
closeBtn.Text = "—"
closeBtn.TextColor3 = Color3.new(1, 1, 1)
closeBtn.Font = Enum.Font.SourceSansBold
closeBtn.TextSize = 24
closeBtn.BackgroundColor3 = Color3.fromRGB(150, 0, 0)
closeBtn.Parent = titleBar

-- Content Frame
local contentFrame = Instance.new("Frame")
contentFrame.Size = UDim2.new(1, 0, 1, -40)
contentFrame.Position = UDim2.new(0, 0, 0, 40)
contentFrame.BackgroundTransparency = 1
contentFrame.Parent = mainFrame

-- Player List
local playerList = Instance.new("ScrollingFrame")
playerList.Size = UDim2.new(0.5, -10, 1, -10)
playerList.Position = UDim2.new(0, 5, 0, 5)
playerList.CanvasSize = UDim2.new(0, 0, 0, 0)
playerList.ScrollBarThickness = 6
playerList.BackgroundColor3 = Color3.fromRGB(40, 40, 40)
playerList.BorderSizePixel = 0
playerList.Parent = contentFrame

local UIListLayout = Instance.new("UIListLayout", playerList)
UIListLayout.Padding = UDim.new(0, 5)
UIListLayout.SortOrder = Enum.SortOrder.LayoutOrder

-- Buttons Frame
local buttonFrame = Instance.new("Frame")
buttonFrame.Size = UDim2.new(0.5, -10, 1, -10)
buttonFrame.Position = UDim2.new(0.5, 5, 0, 5)
buttonFrame.BackgroundTransparency = 1
buttonFrame.Parent = contentFrame

-- Mini Button
local miniBtn = Instance.new("TextButton")
miniBtn.Size = UDim2.new(0, 100, 0, 40)
miniBtn.Position = UDim2.new(0, 10, 0, 10)
miniBtn.Text = "📂 Open"
miniBtn.TextColor3 = Color3.new(1, 1, 1)
miniBtn.Font = Enum.Font.SourceSansBold
miniBtn.TextSize = 18
miniBtn.BackgroundColor3 = Color3.fromRGB(0, 150, 0)
miniBtn.Visible = false
miniBtn.Parent = screenGui

closeBtn.MouseButton1Click:Connect(function()
	screenGui.Enabled = false
	miniBtn.Visible = true
end)

miniBtn.MouseButton1Click:Connect(function()
	screenGui.Enabled = true
	miniBtn.Visible = false
end)

-- ========= FUNCTIONS =========
local function createBtn(txt, callback)
	local btn = Instance.new("TextButton")
	btn.Size = UDim2.new(1, -20, 0, 40)
	btn.Position = UDim2.new(0, 10, 0, 0)
	btn.BackgroundColor3 = Color3.fromRGB(70, 70, 70)
	btn.Text = txt
	btn.TextColor3 = Color3.new(1, 1, 1)
	btn.Font = Enum.Font.SourceSansBold
	btn.TextSize = 18
	btn.Parent = buttonFrame
	btn.MouseButton1Click:Connect(callback)
	local corner = Instance.new("UICorner", btn)
	corner.CornerRadius = UDim.new(0, 8)
	return btn
end

-- Teleport to Player
local function refreshPlayerList()
	playerList:ClearAllChildren()
	local layout = UIListLayout:Clone()
	layout.Parent = playerList
	for _, plr in ipairs(Players:GetPlayers()) do
		if plr ~= LocalPlayer then
			local tpBtn = Instance.new("TextButton")
			tpBtn.Size = UDim2.new(1, -10, 0, 30)
			tpBtn.BackgroundColor3 = Color3.fromRGB(80, 80, 80)
			tpBtn.Text = "Teleport to " .. plr.Name
			tpBtn.TextColor3 = Color3.new(1, 1, 1)
			tpBtn.Font = Enum.Font.SourceSans
			tpBtn.TextSize = 16
			tpBtn.Parent = playerList
			tpBtn.MouseButton1Click:Connect(function()
				if LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart") and plr.Character and plr.Character:FindFirstChild("HumanoidRootPart") then
					LocalPlayer.Character.HumanoidRootPart.CFrame = plr.Character.HumanoidRootPart.CFrame + Vector3.new(2,0,0)
				end
			end)
			local corner = Instance.new("UICorner", tpBtn)
			corner.CornerRadius = UDim.new(0, 6)
		end
	end
end

Players.PlayerAdded:Connect(refreshPlayerList)
Players.PlayerRemoving:Connect(refreshPlayerList)
refreshPlayerList()

-- Leave Game (no jumpscare)
createBtn("🚪 Leave Game", function()
	LocalPlayer:Kick("👋")
end)

-- Copy Discord Link
createBtn("📋 Copy Discord", function(btn)
	setclipboard("https://discord.gg/argiandev")
	btn.Text = "✅ Copied!"
	task.delay(2, function()
		btn.Text = "📋 Copy Discord"
	end)
end)

-- ========= KEY SYSTEM =========
local keyGui = Instance.new("ScreenGui")
keyGui.Name = "KeySystem"
keyGui.Parent = CoreGui

local keyFrame = Instance.new("Frame")
keyFrame.Size = UDim2.new(0, 300, 0, 150)
keyFrame.Position = UDim2.new(0.5, -150, 0.5, -75)
keyFrame.BackgroundColor3 = Color3.fromRGB(25, 25, 25)
keyFrame.Active = true
keyFrame.Draggable = true
keyFrame.Parent = keyGui

local UICornerKey = Instance.new("UICorner", keyFrame)
UICornerKey.CornerRadius = UDim.new(0, 10)

local keyLabel = Instance.new("TextLabel")
keyLabel.Size = UDim2.new(1, 0, 0, 40)
keyLabel.BackgroundTransparency = 1
keyLabel.Text = "Enter Secret Key"
keyLabel.TextColor3 = Color3.new(1, 1, 1)
keyLabel.Font = Enum.Font.SourceSansBold
keyLabel.TextSize = 20
keyLabel.Parent = keyFrame

local keyBox = Instance.new("TextBox")
keyBox.Size = UDim2.new(1, -20, 0, 40)
keyBox.Position = UDim2.new(0, 10, 0, 50)
keyBox.BackgroundColor3 = Color3.fromRGB(50, 50, 50)
keyBox.TextColor3 = Color3.new(1, 1, 1)
keyBox.PlaceholderText = "Type your key..."
keyBox.Font = Enum.Font.SourceSans
keyBox.TextSize = 18
keyBox.Parent = keyFrame

local UICornerBox = Instance.new("UICorner", keyBox)
UICornerBox.CornerRadius = UDim.new(0, 8)

local submitBtn = Instance.new("TextButton")
submitBtn.Size = UDim2.new(0.5, -15, 0, 40)
submitBtn.Position = UDim2.new(0, 10, 0, 100)
submitBtn.BackgroundColor3 = Color3.fromRGB(0, 120, 200)
submitBtn.Text = "Submit"
submitBtn.TextColor3 = Color3.new(1, 1, 1)
submitBtn.Font = Enum.Font.SourceSansBold
submitBtn.TextSize = 18
submitBtn.Parent = keyFrame

local UICornerSubmit = Instance.new("UICorner", submitBtn)
UICornerSubmit.CornerRadius = UDim.new(0, 8)

-- secret key
local SECRET_KEY = "ARGIAN"

submitBtn.MouseButton1Click:Connect(function()
	if keyBox.Text == SECRET_KEY then
		screenGui.Enabled = true   -- enable yung tunay na GUI
		keyGui.Enabled = false     -- i-hide lang, wag i-destroy
	else
		keyBox.Text = ""
		keyBox.PlaceholderText = "❌ Wrong key!"
	end
end)
